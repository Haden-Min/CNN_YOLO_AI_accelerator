# Scripts

Offline tooling belongs here.

Planned scripts:

- Darknet `.cfg` / `.weights` parsing
- BatchNorm folding
- INT8 quantization
- golden-output generation
- binary parameter export
- test-vector generation

Generated outputs should go to ignored artifact directories, not into Git.
