# Models

Model notes and lightweight metadata belong here.

Do not commit large pretrained weights or generated parameter binaries. Keep
downloaded and generated artifacts in ignored local directories such as:

- `models/raw/`
- `models/generated/`

The expected starting point is the Darknet-format YOLOv3-Tiny configuration and
weights.
