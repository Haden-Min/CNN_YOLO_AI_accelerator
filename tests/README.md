# Tests

Verification assets belong here.

Planned test layers:

- Python golden-model tests
- RTL simulation vectors
- block-level RTL tests
- PS/PL transfer smoke tests
- board-level integration checks

The first useful test is a single deterministic INT8 convolution layer compared
against a Python reference output.
